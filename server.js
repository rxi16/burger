var express = require('express');
var exphbs = require('express-handlebars');
var MO = require('method-override');
var bodyParser = require('body-parser');
// ?do we require mysql?

// ? do we need to declare the port?
var port = process.env.PORT || 3306;

var app = express();

app.get('/: /: /: ');

app.listen(port);