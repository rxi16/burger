<!-- do i need !DOCTYPE tag here?-->
<h1>Burgers</h1>
<h2>Burgers that are not devoured</h2>
<ul>
  <!--why are we using burgers rather than burger (cats rather than cat?)-->
  {{#each burgers}}
    <!--looks like logic statement inside html file-->
    {{#unless devoured}}
      <!--{{> cats/cat-block sleep=true}} we don't have burger-block file-->
    {{/unless}}
  {{/each}}
</ul>
<h2>Burgers that are devoured</h2>
<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devour=false}}
    {{/if}}
  {{/each}}
</ul>
<h2>Add a burger</h2>
<form class="create-form">
  <div class="form-group">
    <label for="burge">Burger Name:</label>
    <input type="text" id="bu" name="burger_name">
  </div>
  <div class="form-group">
    <label for="devou">Devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> Devoured!<br>
    <input type="radio" name="devoured" value="0"> Hungry
  </div>
  <button type="submit">Add Burger</button>
</form>

<!--burger-block code below-->
<!--<li>
  {{burger_name}}
  <button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
    {{#if devour}}Time to devour{{else}}Not hungry anymore{{/if}}
  </button>
</li>-->